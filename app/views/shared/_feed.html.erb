<% if @feed_items.any? %>
  <ol class="microposts">
    <%# @feed_itemsはMicropostクラスを持っているので、_micropost.html.erbが呼ばれる %>
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>